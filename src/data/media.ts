/**
 * Named image paths that get swapped for real photography later. Keeping
 * these here means the swap is a one-line edit instead of a hunt through
 * every component that renders the hero or product photos.
 */

/** Aerial stock photo (licensed placeholder) — TODO(MNJ): replace with the
 *  owner's own Latur godown photography once it exists. See BUILD-REPORT.md. */
export const HERO_IMAGE = "/images/hero-stock.jpg";
